# JUICY

A toy NES emulator for OSX in C++.

Note: some ROM file are missing because of licensing. You should be able to find them online easily enough.

## Running

`./build.sh 0` - debug (OSX)

`./build.sh 1` - release (OSX)

`./build.sh 2` - performance (headless)
